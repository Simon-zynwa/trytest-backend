# 🔐 加密工具完整使用指南

## 📚 目录
- [一、关于"只会用就行"的问题](#一关于只会用就行的问题)
- [二、三种加密方式对比](#二三种加密方式对比)
- [三、使用场景速查表](#三使用场景速查表)
- [四、快速使用模板](#四快速使用模板)
- [五、完整API列表](#五完整api列表)

---

## 一、关于"只会用就行"的问题

### 🎯 直接回答：**分阶段学习**

你的想法是**对的**！作为初学者或业务开发者：

#### ✅ **初级阶段（现在）：会用就够了**
- 复制粘贴工具类 ✅
- 知道什么场景用什么加密 ✅
- 按模板调用API ✅
- **不需要深入研究数学原理** ✅

#### 🔄 **进阶阶段（工作1-2年后）：理解核心概念**
- 理解对称加密 vs 非对称加密的区别
- 理解为什么密码不能用MD5
- 理解数字签名的原理
- 知道什么时候该用什么加密

#### 🚀 **高级阶段（资深开发）：深入原理**
- 研究加密算法的数学原理
- 自己实现加密工具
- 处理安全漏洞和攻击

---

### 💡 **我的建议**

```
┌─────────────────────────────────────────┐
│  阶段1（你现在）：会用（1-2周）         │
│  ✓ 复制工具类                           │
│  ✓ 记住使用场景                         │
│  ✓ 能在项目中正确使用                   │
└─────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────┐
│  阶段2（3-6个月后）：理解概念           │
│  ✓ 理解对称加密、非对称加密、哈希的区别 │
│  ✓ 知道为什么这么用                     │
│  ✓ 能选择合适的加密方式                 │
└─────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────┐
│  阶段3（1-2年后）：深入研究（可选）     │
│  ✓ 研究加密原理                         │
│  ✓ 处理安全问题                         │
│  ✓ 架构设计                             │
└─────────────────────────────────────────┘
```

### ⚠️ **但你必须知道的核心概念**

即使只是"会用"，也要记住这些：

| 加密类型 | 能否解密 | 密钥 | 适用场景 |
|---------|---------|-----|---------|
| **AES（对称加密）** | ✅ 可以 | 1个密钥 | 加密可恢复的数据 |
| **RSA（非对称加密）** | ✅ 可以 | 公钥+私钥 | 数字签名、密钥交换 |
| **Hash（哈希）** | ❌ 不可以 | 无密钥 | 密码存储、数据校验 |

---

## 二、三种加密方式对比

### 🔹 AES - 对称加密（可逆）

```java
// 特点：一个密钥，快速，适合大数据
String encrypted = AESUtil.encrypt("敏感数据", key);
String decrypted = AESUtil.decrypt(encrypted, key);
```

**使用场景：**
- ✅ 加密身份证号、银行卡号
- ✅ 加密文件
- ✅ 加密数据库敏感字段
- ❌ 不适合密码存储（用Hash）

---

### 🔹 RSA - 非对称加密（可逆）

```java
// 特点：公钥+私钥，慢，适合小数据
String encrypted = RSAUtil.encryptByPublicKey("数据", publicKey);
String decrypted = RSAUtil.decryptByPrivateKey(encrypted, privateKey);
```

**使用场景：**
- ✅ 数字签名
- ✅ HTTPS证书
- ✅ 密钥交换
- ✅ 登录token签名
- ❌ 不适合大文件加密（太慢）
- ❌ 不适合密码存储（用Hash）

---

### 🔹 Hash - 单向加密（不可逆）

```java
// 特点：不可逆，相同输入永远得到相同输出
String hash = HashUtil.sha256("密码123");
// 无法从hash还原出"密码123"
```

**使用场景：**
- ✅ 密码存储（推荐）
- ✅ 文件完整性校验
- ✅ 数字指纹
- ✅ 防止数据篡改
- ❌ 不适合需要解密的数据

---

## 三、使用场景速查表

### 📋 常见业务场景

| 场景 | 推荐方案 | 原因 |
|-----|---------|-----|
| **用户密码存储** | Hash (SHA-256+盐) | 不需要知道原密码，只需验证 |
| **身份证号存储** | AES | 需要展示原始数据 |
| **银行卡号存储** | AES | 需要展示原始数据 |
| **手机号存储** | AES | 需要展示原始数据 |
| **文件上传加密** | AES | 需要下载时解密 |
| **API签名验证** | RSA数字签名 | 防止数据被篡改 |
| **登录Token** | RSA签名 | 验证token真实性 |
| **文件完整性校验** | Hash (MD5/SHA-256) | 确保文件未被篡改 |
| **前后端数据传输** | RSA+AES混合 | RSA传输AES密钥，AES加密数据 |

---

## 四、快速使用模板

### 1️⃣ AES加密（对称加密）

```java
// 注入配置
@Autowired
private AESConfig aesConfig;

// 加密
String encrypted = AESUtil.encrypt("敏感数据", aesConfig.getSecretKey());
user.setIdCard(encrypted);  // 存到数据库

// 解密
String decrypted = AESUtil.decrypt(user.getIdCard(), aesConfig.getSecretKey());
System.out.println("身份证号：" + decrypted);
```

### 2️⃣ RSA加密（非对称加密）

```java
// 注入配置
@Autowired
private RSAConfig rsaConfig;

// 场景1：公钥加密，私钥解密
String encrypted = RSAUtil.encryptByPublicKey("数据", rsaConfig.getPublicKey());
String decrypted = RSAUtil.decryptByPrivateKey(encrypted, rsaConfig.getPrivateKey());

// 场景2：数字签名
String signature = RSAUtil.sign("重要数据", rsaConfig.getPrivateKey());
boolean valid = RSAUtil.verify("重要数据", signature, rsaConfig.getPublicKey());
```

### 3️⃣ Hash加密（单向加密）

```java
// 场景1：密码存储（推荐）
String encryptedPassword = HashUtil.encryptPassword("123456");
userService.save(user.setPassword(encryptedPassword));

// 场景2：密码验证
boolean correct = HashUtil.verifyPassword("123456", user.getPassword());

// 场景3：文件校验
String fileHash = HashUtil.sha256(fileContent);
if (fileHash.equals(expectedHash)) {
    System.out.println("文件完整");
}
```

---

## 五、完整API列表

### 🔐 AES加密API

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/user/aes/encrypt` | POST | 加密文本 |
| `/user/aes/decrypt` | POST | 解密文本 |
| `/user/aes/generateKey` | GET | 生成密钥 |

**测试示例：**
```bash
# 加密
POST http://localhost:8080/user/aes/encrypt?text=hello123

# 解密
POST http://localhost:8080/user/aes/decrypt?cipherText=xxx
```

---

### 🔑 RSA加密API

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/user/rsa/generateKeyPair` | GET | 生成密钥对 |
| `/user/rsa/encryptByPublic` | POST | 公钥加密 |
| `/user/rsa/decryptByPrivate` | POST | 私钥解密 |
| `/user/rsa/sign` | POST | 数字签名 |
| `/user/rsa/verify` | POST | 验证签名 |

**测试示例：**
```bash
# 1. 生成密钥对
GET http://localhost:8080/user/rsa/generateKeyPair

# 2. 公钥加密
POST http://localhost:8080/user/rsa/encryptByPublic?text=hello

# 3. 私钥解密
POST http://localhost:8080/user/rsa/decryptByPrivate?cipherText=xxx

# 4. 数字签名
POST http://localhost:8080/user/rsa/sign?data=important

# 5. 验证签名
POST http://localhost:8080/user/rsa/verify?data=important&signature=xxx
```

---

### #️⃣ Hash加密API

| 接口 | 方法 | 说明 |
|-----|------|-----|
| `/user/hash/md5` | POST | MD5加密 |
| `/user/hash/sha256` | POST | SHA-256加密 |
| `/user/hash/encryptPassword` | POST | 密码加密（推荐） |
| `/user/hash/verifyPassword` | POST | 验证密码 |

**测试示例：**
```bash
# MD5加密
POST http://localhost:8080/user/hash/md5?text=hello

# SHA-256加密
POST http://localhost:8080/user/hash/sha256?text=hello

# 密码加密
POST http://localhost:8080/user/hash/encryptPassword?password=123456

# 验证密码
POST http://localhost:8080/user/hash/verifyPassword?password=123456&storedHash=xxx$yyy
```

---

## 六、实战案例

### 案例1：用户注册 - 密码存储

```java
@PostMapping("/register")
public Result register(@RequestBody User user) {
    // ✅ 推荐：使用Hash加密密码
    String encryptedPassword = HashUtil.encryptPassword(user.getPassword());
    user.setPassword(encryptedPassword);
    userService.save(user);
    return Result.success();
}

@PostMapping("/login")
public Result login(@RequestBody User user) {
    User dbUser = userService.findByUsername(user.getUsername());
    // ✅ 验证密码
    if (HashUtil.verifyPassword(user.getPassword(), dbUser.getPassword())) {
        return Result.success("登录成功");
    }
    return Result.fail("密码错误");
}
```

### 案例2：实名认证 - 身份证加密

```java
@PostMapping("/realname")
public Result realname(@RequestBody RealNameDto dto) {
    // ✅ 使用AES加密身份证号
    String encryptedIdCard = AESUtil.encrypt(dto.getIdCard(), aesConfig.getSecretKey());
    user.setIdCard(encryptedIdCard);
    userService.update(user);
    return Result.success();
}

@GetMapping("/getUserInfo")
public Result getUserInfo() {
    User user = userService.getCurrentUser();
    // ✅ 解密后返回
    String idCard = AESUtil.decrypt(user.getIdCard(), aesConfig.getSecretKey());
    // 脱敏展示：显示前6后4位
    String masked = idCard.substring(0, 6) + "********" + idCard.substring(14);
    return Result.success(masked);
}
```

### 案例3：API签名验证

```java
@PostMapping("/payment")
public Result payment(@RequestBody PaymentDto dto, @RequestParam String signature) {
    // ✅ 验证签名，防止数据被篡改
    String data = dto.toJsonString();
    boolean valid = RSAUtil.verify(data, signature, rsaConfig.getPublicKey());
    
    if (!valid) {
        return Result.fail("签名验证失败，数据可能被篡改");
    }
    
    // 处理支付逻辑...
    return Result.success();
}
```

---

## 七、常见问题

### Q1: 密码为什么不能用AES加密？
A: 因为AES可以解密，万一密钥泄露，所有密码都会被破解。Hash是单向的，即使泄露也无法还原密码。

### Q2: MD5不是不安全吗，为什么还要学？
A: MD5用于密码已经不安全，但用于**文件校验**还是可以的（速度快）。密码请用SHA-256+盐。

### Q3: 什么时候用RSA，什么时候用AES？
A:
- **RSA**：数据小、需要签名、密钥交换
- **AES**：数据大、速度要求高、对称场景

### Q4: 配置文件中的密钥会不会泄露？
A: 开发环境可以，**生产环境必须用环境变量或密钥管理系统**。

### Q5: 能同时用多种加密吗？
A: 可以！比如HTTPS就是RSA+AES混合：
1. RSA传输AES密钥
2. AES加密实际数据

---

## 八、学习路线建议

### 🎯 第1周：会用（你现在这里）
- [x] 复制工具类到项目
- [x] 测试所有API接口
- [x] 记住使用场景速查表
- [x] 完成1-2个实战案例

### 🎯 第2-4周：理解
- [ ] 理解对称加密、非对称加密、Hash的区别
- [ ] 理解为什么密码不能用AES
- [ ] 理解数字签名的作用
- [ ] 能根据业务场景选择合适的加密方式

### 🎯 第2-3个月：实践
- [ ] 在真实项目中应用
- [ ] 处理各种边界情况
- [ ] 学习安全最佳实践

### 🎯 半年后：深入（可选）
- [ ] 研究加密算法原理
- [ ] 学习密码学基础
- [ ] 了解安全攻防

---

## 九、总结

### ✅ 你现在需要做的

1. **保存这份文档**，随时查阅
2. **测试所有API接口**，确保能用
3. **记住使用场景速查表**
4. **复制模板代码**到你的项目

### ✅ 你现在不需要做的

1. ❌ 研究AES的数学原理
2. ❌ 研究RSA的算法实现
3. ❌ 深入密码学理论
4. ❌ 自己实现加密算法

### 💡 记住这句话

> **"先会用，再理解，最后深入"**  
> 就像开车，先学会开，再理解原理，最后才是修车。

---

## 十、快捷命令

```bash
# 启动项目
cd /Users/yangzhaohui/Desktop/trytest-backend
mvn spring-boot:run

# 测试加密API（推荐用Postman）
# 或者访问 Swagger UI: http://localhost:8080/swagger-ui.html
```

祝学习顺利！🎉
